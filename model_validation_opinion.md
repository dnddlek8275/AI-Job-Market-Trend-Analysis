# 모델 검증 의견서

검증일: 2026-05-31

## 1. 검증 목적

본 문서는 전달받은 수요예측 모델 노트북과 결과 보고서가 실제 모델 학습 및 테스트 검증 결과로 볼 수 있는지 확인하기 위해 작성했다.

검증 대상은 다음 네 개 노트북이다.

| 구분 | 파일 |
| --- | --- |
| Logistic Regression | `LogisticRegression.ipynb` |
| Random Forest | `RandomForestClassifier.ipynb` |
| XGBoost | `XGBoost.ipynb` |
| LightGBM | `LightGBM.ipynb` |

## 2. 검증 가능 여부

현재 작업 폴더에 모델 실행에 필요한 주요 파일이 존재하므로 검증 가능하다.

| 파일 | 확인 내용 |
| --- | --- |
| `jswBad_1.csv` | 존재함, 15,000행 86컬럼 |
| `jswBad_2.csv` | 존재함, 15,000행 34컬럼 |
| `LogisticRegression.ipynb` | 실행 출력 저장됨 |
| `RandomForestClassifier.ipynb` | 실행 출력 저장됨 |
| `XGBoost.ipynb` | 실행 출력 저장됨 |
| `LightGBM.ipynb` | 실행 출력 저장됨 |

또한 현재 환경에서 다음 패키지 import가 가능함을 확인했다.

| 패키지 | 버전 |
| --- | --- |
| scikit-learn | 1.7.2 |
| xgboost | 3.2.0 |
| lightgbm | 4.6.0 |
| pandas | 2.3.3 |
| numpy | 2.3.5 |

따라서 저장된 노트북 출력 확인뿐 아니라, 핵심 학습/평가 로직을 현재 환경에서 재실행해 성능 지표를 재계산하는 검증도 가능하다.

## 3. 데이터 분리 확인

노트북들은 공통적으로 `high_salary`를 예측 타깃으로 사용한다.

학습/테스트 분리는 `posting_month`의 정렬 순서를 기준으로 마지막 월을 테스트셋으로 두는 방식이다.

| 항목 | 값 |
| --- | --- |
| 전체 데이터 수 | 15,000개 |
| 학습 데이터 수 | 14,105개 |
| 테스트 데이터 수 | 895개 |
| 테스트셋 실제 `high_salary=1` 건수 | 222개 |

`jswBad_2.csv` 기준 테스트 월은 `202504`로 확인된다.

단, `LogisticRegression.ipynb`에서 사용하는 `jswBad_1.csv`는 `posting_month`가 스케일링된 값으로 저장되어 있어 테스트 월이 원래 라벨 `202504`로 직접 표시되지는 않는다. 이 노트북에서는 스케일링된 `posting_month` 값 중 최댓값을 테스트셋으로 사용한다.

## 4. 저장 결과와 재계산 결과 비교

노트북에 저장된 결과와 현재 환경에서 동일 데이터 및 동일 주요 파라미터로 재계산한 결과를 비교했다.

| 모델 | 저장 결과 Accuracy | 재계산 Accuracy | 저장 결과 F1 | 재계산 F1 | 판단 |
| --- | ---: | ---: | ---: | ---: | --- |
| Logistic Regression | 0.9229 | 0.9229 | 0.8617 | 0.8617 | 일치 |
| RandomForestClassifier | 0.9050 | 0.9039 | 0.8195 | 0.8170 | 근소 차이 |
| XGBoost | 0.9296 | 0.9285 | 0.8712 | 0.8694 | 근소 차이 |
| LightGBM | 0.9330 | 0.9330 | 0.8755 | 0.8755 | 일치 |

현재 환경에서 재계산한 전체 성능은 다음과 같다.

| 모델 | Accuracy | Precision | Recall | F1 | ROC-AUC | 예측 0건수 | 예측 1건수 | 평균 예측 확률 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| Logistic Regression | 0.9229 | 0.7762 | 0.9685 | 0.8617 | 0.9847 | 618 | 277 | 0.3031 |
| RandomForestClassifier | 0.9039 | 0.7742 | 0.8649 | 0.8170 | 0.9677 | 647 | 248 | 0.2691 |
| XGBoost | 0.9285 | 0.7948 | 0.9595 | 0.8694 | 0.9853 | 627 | 268 | 0.2906 |
| LightGBM | 0.9330 | 0.8115 | 0.9505 | 0.8755 | 0.9852 | 635 | 260 | 0.2775 |

## 5. 검증 결과 해석

전달받은 노트북과 보고서는 단순 설명 문서가 아니라, 모델 학습 및 테스트 평가 결과를 포함한 검증 자료로 볼 수 있다.

근거는 다음과 같다.

- 각 노트북에 실행 카운트와 출력 결과가 저장되어 있다.
- 성능 지표가 테스트셋 기준으로 산출되어 있다.
- 테스트셋 예측값과 예측 확률 요약이 포함되어 있다.
- 현재 환경에서 핵심 로직을 재계산했을 때 주요 결과가 대체로 재현된다.

다만 RandomForestClassifier와 XGBoost는 저장 결과와 재계산 결과가 완전히 일치하지 않고 소폭 차이가 있었다. 현재 확인된 범위에서는 차이가 크지 않아 모델 순위나 최종 결론을 바꿀 정도는 아니다.

차이 발생 가능성이 있는 부분은 다음과 같다.

- 라이브러리 버전 차이
- 트리 기반 모델의 내부 학습 구현 차이
- 병렬 처리 설정 `n_jobs=-1`에 따른 미세한 결과 차이
- 노트북 저장 당시 실행 환경과 현재 실행 환경 차이

## 6. 검증상 주의사항

현재 비교는 모델 간 완전히 동일 조건의 비교라고 보기는 어렵다.

| 항목 | 내용 |
| --- | --- |
| 데이터셋 차이 | Logistic Regression은 `jswBad_1.csv`, 나머지 모델은 `jswBad_2.csv` 사용 |
| 컬럼 수 차이 | `jswBad_1.csv`는 86컬럼, `jswBad_2.csv`는 34컬럼 |
| 입력 변수 | 모든 모델이 `high_salary`를 제외한 전체 컬럼을 입력으로 사용 |
| 시간 변수 | `posting_month`, `quarter`가 입력 변수에 포함됨 |
| 검증 방식 | 마지막 월 단일 테스트셋 기준 검증 |

따라서 이 결과는 현재 자료 기준의 모델 성능 검증으로는 사용할 수 있으나, 최종 모델 확정 전에는 동일 데이터셋과 동일 입력 컬럼 기준의 재검증을 추가하는 것이 더 안전하다.

## 7. 최종 검증 의견

현재 제공된 자료는 모델을 실제로 학습하고 테스트셋으로 평가한 결과를 포함하고 있으므로, 검증 대상 자료로 인정할 수 있다.

핵심 성능 지표를 재계산한 결과, Logistic Regression과 LightGBM은 저장 결과와 동일하게 재현되었고, RandomForestClassifier와 XGBoost는 아주 근소한 차이만 확인되었다.

현재 재계산 기준으로도 LightGBM은 Accuracy, Precision, F1에서 가장 높은 값을 보이며, 최종 추천 모델이라는 기존 결론은 유지된다.

단, Logistic Regression과 나머지 모델이 서로 다른 전처리 데이터셋을 사용하고 있으므로, 최종 제출 또는 운영 모델 확정 단계에서는 동일 데이터셋, 동일 입력 컬럼, 동일 검증 기준으로 한 번 더 비교하는 것을 권장한다.

## 8. 제출용 요약 문장

전달받은 모델 노트북과 결과 보고서를 검토한 결과, 해당 자료는 실제 모델 학습 및 2025년 4월 테스트셋 기준 평가 결과를 포함하고 있어 검증 가능한 자료로 판단된다. 현재 환경에서 핵심 성능 지표를 재계산한 결과 Logistic Regression과 LightGBM은 저장 결과와 동일하게 재현되었고, RandomForestClassifier와 XGBoost는 근소한 차이가 있었으나 최종 모델 판단에는 영향을 주지 않는 수준이다. 현재 기준 최종 추천 모델은 LightGBM이며, 다만 모델 간 사용 데이터셋이 일부 다르므로 최종 확정 전 동일 조건 재검증을 권장한다.
